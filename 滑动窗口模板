string minWindow(string s, string t) {
int left=0,right=0;
int valid=0;
unordered_map<char,int> need,window;
for(auto i:t) need[i]++;
while(right<s.size()){
//不论如何窗口都是要一直增大的
    char c=s[right];
// 进行窗口内数据的一系列更新

    right++;

while(//window needs shrink){
    char d=s[left];
 //更新结果
 //更新窗口（进行窗口内数据的一系列更新）
    left++;

}

1.增大窗口时，要更新窗口内的什么数据？

2.什么时候窗口要缩小（shrink）？

3.窗口缩小时，更新窗口内的什么数据？

4.什么时候更新结果？
